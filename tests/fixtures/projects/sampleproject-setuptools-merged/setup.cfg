# SPDX-FileCopyrightText: 2026-present Arthit Suriyawongkul
# SPDX-License-Identifier: CC0-1.0
#
# Contributes packages/package_dir -- package-data comes from
# pyproject.toml instead (see pyproject.toml in this directory).

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src
